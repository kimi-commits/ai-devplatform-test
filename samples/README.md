# Samples

## Phase0-MafDemo

Phase 0 的 Microsoft Agent Framework 驗證 Demo,獨立於 `AI.Host` 之外,接的是 NVIDIA NIM
(OpenAI-Compatible API)。詳細執行方式見根目錄 `README.md` 的「Phase 0」一節。

## 之後

放置示範用的 Workspace 專案(對應規格書 v1 第 6 節的 Workspace 概念),用來驗證 Phase 1
的單一序列 Pipeline:`Planner → Coder → Reviewer → QA → Build`。
